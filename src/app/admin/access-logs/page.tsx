import AdminPageHeader from "@/features/admin/components/AdminPageHeader";

export default function AdminAccessLogsPage() {
    return (
        <AdminPageHeader
            title="접근 로그"
            description="접근 로그 전체 조회 API가 구현되면 로그 테이블과 위험도 필터를 연결할 예정입니다."
            badge="준비 중"
        />
    );
}
